apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    purpose: service
spec:
  selector:
    app: nginx
  ports:
  - port: 8081
    targetPort: 8081
    nodePort: 30861
  type: NodePort
